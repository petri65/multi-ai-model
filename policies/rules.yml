gpu_required: true
timestamp_quantization: 1s
seq_len: 240
horizons: [10,30,60,90,120,240]
model: bayesian_lstm
uncertainty: mc_dropout
kelly: distributional
egress:
  secrets: deny
  raw_data: deny
gpu_policy:
  require_auto_device: true
  allowed_devices: ["cuda", "mps", "auto", "cpu"]
  prefer: ["cuda", "mps"]
  forbid_default_cpu: true
