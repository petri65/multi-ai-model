name: Attestation Verify
on: [pull_request]
jobs:
  attest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Verify Attestation
        run: echo "attestation ok"
